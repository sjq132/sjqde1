# 学习笔记

## 一、AI 工具学习

在本阶段的学习中，我主要使用了以下 AI 工具辅助开发和学习：

| 工具名称 | 用途 | 使用体验 |
|---------|------|---------|
| [腾讯元宝] | 解答 Git 命令疑问、梳理 Markdown 格式、辅助撰写学习笔记 | 回答准确，中文理解好，适合快速查漏补缺 |
| [Cursor] | 辅助编写 Python 图像处理脚本、生成代码片段 | 代码补全智能，能根据注释直接生成可用函数 |

**总结**：AI 工具在查阅资料、代码生成、文档撰写三个环节都显著提升了效率，尤其是将模糊的需求描述快速转化为结构化内容的能力非常实用。

---

 ## 二、拟选用的 Harness 和模型

### Harness 方案
我拟选用 **AGENTS.md + PROGRESS.md + 本地 Shell 测试门禁** 作为本项目的 harness 方案。

**理由**：
- 轻量简洁，不需要额外部署 CI/CD 服务，个人项目即可运行
- AGENTS.md 明确任务分工与规范，PROGRESS.md 追踪进度，适合迭代式开发
- 本地测试门禁可在每次提交前自动运行单元测试和代码风格检查，保证代码质量

### 模型选型

#### 核心模型：YOLOv8（目标检测）

| 项目 | 说明 |
|------|------|
| 模型名称 | YOLOv8n / YOLOv8s（Ultralytics 官方发布） |
| 模型类型 | 单阶段目标检测模型（One-Stage Object Detection） |
| 选用版本 | 先用 YOLOv8n（轻量版）做快速验证，后续视精度需求升级到 YOLOv8s |
| 训练框架 | Ultralytics YOLOv8 + PyTorch |

**为什么选 YOLOv8 作为核心检测模型：**

1. **任务匹配度高**：本系统需要对零部件表面缺陷（划痕、裂纹、凹陷等）进行定位和分类，YOLOv8 天然支持目标检测 + 分类，一个模型解决两个问题
2. **速度快**：YOLOv8n 在 CPU 上也能达到实时推理速度（~30 FPS），适合产线场景
3. **生态成熟**：Ultralytics 提供了完整的训练/验证/导出 API，文档完善，社区活跃，踩坑容易找到解决方案
4. **部署灵活**：支持导出为 ONNX / TensorRT / OpenVINO 等格式，后续可部署到边缘设备（如 Jetson Nano、工业相机内置芯片）

#### 辅助模型：ResNet-18（缺陷分类 / 迁移学习基线）

| 项目 | 说明 |
|------|------|
| 模型名称 | ResNet-18（预训练权重 + 微调） |
| 模型类型 | 图像分类卷积神经网络 |
| 用途 | 对 YOLOv8 检测出的候选区域做二次分类，降低误检率 |

**为什么加 ResNet-18：**
- YOLOv8 对小目标缺陷可能误检，用 ResNet-18 做二阶段验证可以过滤假阳性
- 预训练权重在 ImageNet 上训练过，微调只需少量标注数据就能达到不错效果
- 模型极小（~11MB），推理快，不会成为系统瓶颈

#### 备选 / 探索模型：Qwen2.5-VL（多模态视觉语言模型）

| 项目 | 说明 |
|------|------|
| 模型名称 | Qwen2.5-VL-7B-Instruct |
| 模型类型 | 多模态大模型（Vision-Language Model） |
| 用途 | 探索性方案——用自然语言描述缺陷类型，辅助生成检测报告 |

**选用理由：**
- 支持图像理解 + 中文对话，可直接让模型"看图说话"描述缺陷
- 适合做检测结果的**自然语言报告生成**（如："该零件左上方存在一条长约 3mm 的划痕，深度较浅，建议返修"）
- 作为课程设计的**扩展功能**展示，体现技术深度

#### 模型选型总结

| 模型 | 角色 | 是否必须 | 优先级 |
|------|------|---------|--------|
| YOLOv8 | 核心缺陷检测 | ✅ 必须 | P0 |
| ResNet-18 | 分类验证（降误检） | ⚠️ 可选（进阶功能） | P1 |
| Qwen2.5-VL | 报告生成（多模态扩展） | ❌ 探索性 | P2 |

**最终决策**：第一阶段先实现 YOLOv8 单模型检测 + Web 界面展示；若时间充裕，依次加入 ResNet-18 二次验证和 Qwen2.5-VL 报告生成模块。

---

## 三、Git 原理与功能

### Git 是什么
Git 是一个**分布式版本控制系统**，每个开发者的本地都有完整的版本历史，不依赖中央服务器也能工作。

### 核心概念
| 概念 | 说明 |
|------|------|
| 工作区（Working Directory） | 当前正在编辑的文件目录 |
| 暂存区（Staging Area） | 临时存放待提交的改动（`git add` 之后） |
| 本地仓库（Local Repository） | 本机存储的版本历史（`git commit` 之后） |
| 远程仓库（Remote Repository） | GitHub 上的仓库（`git push` 推送上去） |

### 常用命令与功能
| 命令 | 功能 |
|------|------|
| `git init` | 初始化本地仓库 |
| `git add <文件>` | 将文件加入暂存区 |
| `git commit -m "说明"` | 将暂存区内容提交到本地仓库 |
| `git push` | 推送到远程仓库 |
| `git pull` | 拉取远程更新到本地 |
| `git log` | 查看提交历史 |
| `git reset / git revert` | 版本回退或撤销 |

### 为什么用 Git
- **版本回溯**：随时回到任意历史版本，不怕改崩代码
- **多人协作**：分支管理，互不覆盖
- **代码备份**：远程仓库即是备份，换电脑也能恢复
- **变更追踪**：每次改动都有记录可查，方便定位问题

---

## 四、选题调研

### 拟选题目
**工业零部件质量智能检测系统**

### 选题背景
在制造业生产线上，传统的人工质检方式存在效率低、主观性强、易疲劳漏检等问题。随着工业 4.0 和智能制造的推进，利用计算机视觉和深度学习技术实现零部件缺陷的自动检测已成为行业刚需。本系统旨在构建一个能够对工业零部件表面缺陷（如划痕、裂纹、凹陷、毛刺等）进行自动识别和分类的原型系统，提升质检效率和准确率。

### 调研情况
我调研了以下类似项目/方案：

| 项目/方案 | 特点 | 参考点 |
|-----------|------|--------|
| YOLOv8 工业缺陷检测 | 单阶段目标检测，速度快，适合实时检测场景 | 参考其模型选型和训练流程 |
| DeepSeek-VL 视觉语言模型 | 支持图像理解，可结合文本描述进行缺陷判断 | 探索多模态方案的可行性 |
| 某开源轴承缺陷检测数据集项目 | 提供标注好的工业零件图像数据 | 作为本系统的测试数据集来源 |

### 为什么选这个题
- **现实意义强**：工业质检是智能制造的核心环节，落地价值明确
- **技术覆盖面广**：涉及图像处理、深度学习、前后端交互，能综合锻炼开发能力
- **可扩展性好**：从单品类检测可扩展到多品类，从离线检测可扩展到实时视频流检测
- **有现成资源**：公开数据集和预训练模型丰富，降低了从零起步的难度

### 预期实现功能
- 支持上传工业零部件图片并进行缺陷检测
- 输出缺陷位置标注图及缺陷类型、置信度等信息
- 提供简单的 Web 交互界面，方便演示和测试
- 检测历史记录存储与查询

---

> 本文档为课程设计第一阶段学习笔记，提交时间：[2026-08-25]
