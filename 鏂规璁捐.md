# 方案设计

## 一、功能需求分析

### 用户需求
- 工厂质检人员需要上传零部件图片，快速获得缺陷检测结果（既要知道"是什么缺陷"，也要知道"缺陷在哪"）。
- 需要直观看到缺陷位置标注（检测框）和类型/置信度判断。
- 需要历史检测记录可供追溯、按条件筛选和统计分析。

### 功能模块划分
| 模块 | 功能描述 | 优先级 |
|------|---------|--------|
| 图片上传模块 | 支持 JPG/PNG 格式图片上传，前端预览 | P0 |
| 缺陷检测模块 | 调用 YOLOv8 模型推理，返回缺陷位置框、类别、置信度 | P0 |
| 结果展示模块 | 在图片上绘制检测框、标签、置信度，叠加显示 | P0 |
| 历史记录模块 | 存储每次检测结果，支持按日期/类型/置信度筛选查询 | P1 |
| 统计报表模块 | 按缺陷类型统计频次，Chart.js 生成饼图/柱状图 | P2 |

## 二、方案论证

### 检测方案对比
| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| 传统图像处理（边缘检测+阈值分割） | 无需训练数据，速度快 | 泛化能力差，对复杂背景和微小缺陷效果差 | ❌ 不采用 |
| 两阶段检测（Faster R-CNN） | 精度高，定位准 | 速度慢，不适合准实时场景 | ❌ 暂不选 |
| **YOLOv8 单阶段检测** | **速度/精度均衡、原生支持定位+分类、部署方便** | 极小目标检测能力略弱 | ✅ **采用** |
| 纯分类模型（ResNet 等整图分类） | 实现简单、成熟 | 无法定位缺陷位置，信息量不足 | ⚠️ 仅作辅助/二次验证 |

**结论**：本系统选择 YOLOv8 作为核心检测模型，正是为了区别于"仅分类"的同类方案，实现"定位+分类"一体化的质检输出。

### 技术栈选择论证
- **后端框架选 Flask 而非 Django**：功能相对聚焦，Flask 轻量灵活、学习成本低，专注算法服务化即可。
- **数据库选 SQLite 而非 MySQL**：课程设计阶段数据量小，SQLite 零配置、单文件部署，便于演示与交付。
- **前端选原生 HTML+JS+Bootstrap 而非 Vue/React**：降低工程复杂度，把精力集中在检测算法核心上。
- **检测模型选 YOLOv8n/s 而非 YOLOv5**：Ultralytics YOLOv8 API 更统一，支持一键导出 ONNX/TensorRT，社区活跃。

## 三、方案规划

### 技术栈总览
| 层次 | 技术选型 | 说明 |
|------|---------|------|
| 前端 | HTML5 + CSS3 + JavaScript + Bootstrap + Chart.js | 响应式布局，快速搭建 |
| 后端 | Python + Flask | 轻量 Web 框架，路由清晰 |
| 算法 | YOLOv8 (Ultralytics) + PyTorch | 核心目标检测模型 |
| 数据库 | SQLite | 本地文件数据库，零配置 |
| 部署 | 本地运行（可选 Docker 容器化） | 方便演示与交付 |

### 外部资源配置
| 资源 | 说明 |
|------|------|
| GPU（可选） | 训练阶段使用 Google Colab 免费 GPU；推理阶段 CPU 即可满足准实时 |
| 模型导出工具 | Ultralytics 内置导出（ONNX / TensorRT），便于后续边缘部署 |

### 数据集来源（规范引用）
本系统采用**东北大学 NEU Surface Defect Database（NEU-DET）** 作为标准数据集。该数据集由东北大学 Song 等学者公开发布，是钢材表面缺陷视觉检测领域广泛使用的基准数据集，具体信息如下：

| 项目 | 说明 |
|------|------|
| 数据集名称 | NEU Surface Defect Database（NEU-DET） |
| 发布单位 | 东北大学（Northeastern University, China） |
| 数据规模 | 6 类典型缺陷，共 1800 张灰度图像（每类 300 张） |
| 缺陷类别 | 裂纹（crack）、夹杂物（inclusion）、斑块（patch）、麻点（pitted_surface）、氧化皮（rolled-in_scale）、划痕（scratch） |
| 图像规格 | 单通道灰度图，200×200 像素 |
| 标注格式 | 提供 PASCAL VOC 格式 XML 边界框标注，可转换为 YOLO 格式（txt） |
| 官方来源 | 东北大学材料电磁过程研究教育部重点实验室：http://faculty.neu.edu.cn/songkechen/ |

> 选择依据：NEU-DET 是针对钢材表面缺陷**同时提供分类标签与边界框标注**的标准数据集，恰好契合本系统"定位+分类"的目标检测定位，是同类研究中引用最广泛的公开基准之一，便于横向对比与结果复现。

**数据划分**：按 8:1:1 划分为训练集 / 验证集 / 测试集（1440 / 180 / 180 张），训练阶段采用翻转、缩放、亮度扰动等在线数据增强提升泛化能力。

### 前端 UI 规划
- **首页**：上传区域 + 检测按钮 + 结果标注展示区（Canvas 叠加检测框）
- **历史页**：表格展示过往检测记录，支持按日期/缺陷类型/置信度筛选
- **统计页**：饼图/柱状图展示缺陷类型分布（Chart.js）

### 后端算法流程
```
图片上传 → 预处理（Resize 640×640, 归一化, 灰度转RGB）
         → YOLOv8 推理（返回 bboxes + class + confidence）
         → 后处理（NMS 去重, 置信度过滤阈值 0.5）
         → 结果写入 SQLite 数据库
         → 返回 JSON + 标注图给前端渲染
```

## 四、技术路线

```
Phase 1: 环境搭建 + 数据集准备（第1周）
  ├── Python + PyTorch + YOLOv8(Ultralytics) 环境配置
  ├── NEU-DET 数据集下载、VOC-XML → YOLO-txt 标注格式转换
  └── 数据增强脚本编写、训练/验证/测试划分

Phase 2: 模型训练与调优（第2-3周）
  ├── YOLOv8n 基线训练（迁移学习, 加载 COCO 预训练权重）
  ├── 超参数调优（lr, batch_size, augmentation）
  ├── 评估指标：mAP@0.5, Precision, Recall, F1
  └── 可选：ResNet-18 二次分类器训练（用于高置信度样本复核）

Phase 3: Web 系统开发（第4-5周）
  ├── Flask 后端 API 开发（/upload, /detect, /history, /stats）
  ├── 前端页面开发（上传/历史/统计三页）
  ├── SQLite 数据库设计与建表
  └── 前后端联调、检测结果可视化

Phase 4: 测试与优化（第6周）
  ├── 功能测试 + 边界测试（非缺陷图、多缺陷图）
  ├── 性能优化（推理加速、结果缓存策略）
  └── 撰写实验报告和用户手册
```

## 五、计划安排

| 周次 | 任务 | 交付物 |
|------|------|--------|
| 第1周 | 环境搭建、NEU-DET 数据集准备、文献调研 | 数据集就绪、环境配置文档 |
| 第2周 | YOLOv8 基线模型训练 | 可运行的检测脚本 |
| 第3周 | 模型调优、评估（mAP/P/R/F1） | 训练日志、评估指标报告 |
| 第4周 | 后端 API + 数据库开发 | Flask 服务可本地运行 |
| 第5周 | 前端页面开发 + 联调 | 完整 Web 系统 |
| 第6周 | 测试、优化、写文档 | 最终交付 + 实验报告 |

## 六、参考文献

1. Jocher, G., et al. (2023). *YOLOv8 by Ultralytics*. Ultralytics. https://github.com/ultralytics/ultralytics
2. Redmon, J., Divvala, S., Girshick, R., & Farhadi, A. (2016). *You Only Look Once: Unified, Real-Time Object Detection*. CVPR.
3. He, K., Zhang, X., Ren, S., & Sun, J. (2016). *Deep Residual Learning for Image Recognition*. CVPR.
4. **Song, K., Yan, Y. (2013). A novel surface defect inspection algorithm for magnetic tile. *Applied Surface Science*.**（NEU 数据集相关方法文献）
5. 东北大学材料电磁过程研究教育部重点实验室. *NEU Surface Defect Database*. http://faculty.neu.edu.cn/songkechen/
6. Ultralytics. (2024). *YOLOv8 Documentation*. https://docs.ultralytics.com/

## 七、差异化小结

本方案相较"仅做缺陷分类"的同类方案，核心差异化定位为三点：
1. **任务范式差异**：采用目标检测（YOLOv8）而非图像分类，输出"类别+位置+置信度"，更贴合产线实际质检需求。
2. **工程闭环差异**：不止于算法脚本，构建完整 B/S 系统，覆盖上传→检测→标注→存储→查询→统计全流程。
3. **多模型协同差异**：以 YOLOv8 为主模型，预留 ResNet-18 二次验证与 Qwen2.5-VL 自然语言报告生成的扩展接口，体现方案的技术深度与可演进性。
