# 方案设计

## 一、功能需求分析

### 用户需求
- 质检人员上传工业零部件图片，快速获得缺陷检测结果
- 直观看到缺陷位置标注和类型判断
- 历史检测记录可供追溯和统计分析

### 功能模块划分
| 模块 | 功能描述 | 优先级 |
|------|---------|--------|
| 图片上传模块 | 支持 JPG/PNG 上传，前端预览 | P0 |
| 缺陷检测模块 | 调用 YOLOv8 推理，返回缺陷位置与类别 | P0 |
| 结果展示模块 | 在图上绘制检测框、标签、置信度 | P0 |
| 历史记录模块 | 存储每次检测结果，支持按日期/类型筛选 | P1 |
| 统计报表模块 | 按缺陷类型统计频次，生成简单图表 | P2 |

## 二、方案论证

### 检测方案对比
| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| 传统图像处理（边缘检测+阈值分割） | 无需训练数据，速度快 | 泛化差，对复杂背景/微小缺陷效果差 | ❌ 不采用 |
| 两阶段检测（Faster R-CNN） | 精度高 | 速度慢，不适合实时 | ❌ 暂不选 |
| 纯分类模型（整图判类） | 简单易实现 | 无法定位缺陷位置 | ⚠️ 仅作辅助对比 |
| **YOLOv8 单阶段检测** | **速度快、精度均衡、可定位** | 小目标略弱 | ✅ **采用（主模型）** |

### 技术栈选择论证
- **后端选 Flask 而非 Django**：功能相对简单，Flask 轻量灵活、学习成本低
- **数据库选 SQLite 而非 MySQL**：课程设计数据量小，SQLite 零配置、单文件部署
- **前端选原生 HTML+JS 而非 Vue/React**：降低复杂度，专注算法核心

## 三、方案规划

### 技术栈总览
| 层次 | 技术选型 | 说明 |
|------|---------|------|
| 前端 | HTML5 + CSS3 + JavaScript + Bootstrap | 响应式布局 |
| 后端 | Python + Flask | 轻量 Web 框架 |
| 算法 | YOLOv8 (Ultralytics) + PyTorch | 核心检测模型 |
| 数据库 | SQLite | 本地文件数据库 |
| 部署 | 本地运行 / 可选 Docker 容器化 | 方便演示交付 |

### 外部资源配置
| 资源 | 说明 |
|------|------|
| GPU（可选） | 训练可用 Google Colab 免费 GPU，推理 CPU 即可 |
| 云存储（可选） | 图片暂存用本地文件系统，后期可接入 OSS |

### 数据集来源
| 数据集 | 规模/类别 | 来源与许可 | 在本项目中的用途 |
|--------|----------|------------|----------------|
| **MVTec AD（主）** | 5354 张高分辨率彩色图，15 类工业对象与纹理 | MVTec Software 官方发布，CC BY-NC-SA 4.0；https://www.mvtec.com/company/research/datasets/mvtec-ad/ | 主训练/测试集，覆盖多类通用零部件 |
| KolektorSDD（辅） | 399 张真实工业电气换向器表面缺陷图 | 卢布尔雅那大学 ViCoS Lab；CC BY-NC-SA 4.0 | 补充真实工业场景验证 |
| PKU-Market-PCB（辅） | 1386 张 PCB 缺陷图，6 类缺陷 | 北京大学深圳研究生院 HRI 实验室 | 扩展 PCB 类零部件检测验证 |

> 说明：MVTec AD 原为标准异常检测基准（图像级/像素级异常分割），本项目将其用于 YOLOv8 监督式目标检测时，需基于其像素级标注自行生成检测框标注（bounding-box），以适配检测范式。

### 前端 UI 规划
- **首页**：上传区域 + 检测按钮 + 结果展示区
- **历史页**：表格展示过往检测记录，支持搜索筛选
- **统计页**：饼图/柱状图展示缺陷类型分布（Chart.js）

### 后端算法流程

图片上传 → 预处理（Resize 640×640, 归一化）  
→ YOLOv8 推理（返回 bboxes + class + confidence）  
→ 后处理（NMS 去重，置信度过滤 > 0.5）  
→ 结果写入数据库  
→ 返回 JSON + 标注图给前端渲染
## 四、技术路线

### 4.1 总Phase 1: 环境搭建 + 数据集准备（第1周）
├── Python 3.10 + PyTorch + CUDA 环境配置
├── Ultralytics YOLOv8 安装与验证
├── MVTec AD 数据集下载（15类，5354张，CC BY-NC-SA 4.0）
├── 基于像素级标注生成 YOLO 检测框标注（bbox txt）
├── 数据集划分：训练集 / 验证集 / 测试集（7:1.5:1.5）
└── 数据增强脚本（旋转、翻转、亮度扰动、MixUp）
Phase 2: 模型训练与调优（第2-3周）
├── YOLOv8n 基线训练（预训练权重 yolov8n.pt）
├── 超参数调优（lr=0.01, batch=16, mosaic=0.5）
├── 评估指标：mAP@0.5, Precision, Recall, F1-Score
├── 模型升级：YOLOv8s 对比实验
└── 可选：ResNet-18 二次分类器训练（过滤 YOLO 假阳性）
Phase 3: Web 系统开发（第4-5周）
├── Flask 后端 API 开发（/upload, /detect, /history, /stats）
├── SQLite 数据库设计与建表（detection_records, defect_types）
├── 前端页面开发（Bootstrap + Chart.js）
│ ├── 首页：上传 + 实时检测展示
│ ├── 历史页：检测记录表格 + 筛选
│ └── 统计页：缺陷类型分布饼图/柱状图
└── 前后端联调 + 标注图叠加渲染
Phase 4: 测试与优化（第6周）
├── 功能测试 + 边界测试（异常图片、超大图片、非工业图）
├── 性能优化（模型推理加速、结果缓存）
├── 可选：Qwen2.5-VL 多模态报告生成模块（探索性扩展）
└── 撰写实验报告与用户手册
### 4.2 核心算法流程图
输入图片
│
▼
前端上传 ──→ 预处理（Resize 640×640 → 归一化 → Tensor 格式转换）
│
▼
YOLOv8 推理（加载 best.pt → 前向传播 → 输出 bboxes + class_id + confidence）
│
▼
后处理（NMS 去重 IoU=0.45 → 置信度过滤 > 0.5）
│
▼
二次验证（可选）：裁剪检测区域 → ResNet-18 推理 → 确认/否决
│
▼
结果存储（检测记录写入 SQLite：时间、图片路径、缺陷列表）
│
▼
前端渲染（返回 JSON + 标注图 OpenCV 绘制检测框+标签 → 展示 + 历史查询）
### 4.3 数据集处理技术路线
MVTec AD 原始数据（像素级异常标注 PNG mask）
│
▼
标注格式转换脚本（Python + OpenCV）
├── 读取 mask → 提取异常区域轮廓
├── 计算最小外接矩形 → 生成 YOLO bbox（class_id, x_center, y_center, w, h）
└── 输出为 YOLO 标准 txt 标注文件（每张图对应一个 .txt）
│
▼
YOLOv8 训练数据目录结构：
dataset/
├── images/
│ ├── train/
│ ├── val/
│ └── test/
└── labels/
├── train/
├── val/
└── test/
### 4.4 技术选型决策路线
需求：通用工业零部件缺陷检测 + Web 可演示
│
├── 检测算法选型
│ ├── 传统 CV（边缘+阈值）→ 淘汰：泛化差
│ ├── 两阶段（Faster R-CNN）→ 淘汰：速度慢
│ ├── 纯分类 CNN → 保留辅助对比，主选淘汰：无法定位
│ └── ✅ YOLOv8 单阶段检测 → 选中：速度+精度+定位兼顾
│
├── 数据集选型
│ ├── NEU-DET（钢材）→ 淘汰：单一材料，与同类方案撞题
│ ├── 自建数据 → 淘汰：标注成本高、数据量不足
│ └── ✅ MVTec AD（通用零部件）→ 选中：多类、权威、像素标注可转检测框
│
├── 系统架构选型
│ ├── 纯算法 Notebook → 淘汰：不可演示业务闭环
│ └── ✅ B/S Web 系统 → 选中：Flask + 原生前端 + SQLite
│
└── 扩展模型选型
├── ResNet-18 → 选中（P1）：二次验证降误检
└── Qwen2.5-VL → 选中（P2）：多模态报告生成探索体技术路线
## 五、计划安排

| 阶段 | 任务 | 交付物 |
|------|------|--------|
| 第一阶段 | 环境搭建、数据集准备、文献调研 | 数据集就绪、环境配置文档 |
| 第二阶段 | YOLOv8 基线模型训练 | 可运行的检测脚本 |
| 第三阶段 | 模型调优、评估 | 训练日志、评估指标报告 |
| 第四阶段 | 后端 API + 数据库开发 | Flask 服务可本地运行 |
| 第五阶段 | 前端页面开发 + 联调 | 完整 Web 系统 |
| 第六阶段 | 测试、优化、写文档 | 最终交付 + 实验报告 |
## 六、参考文献

1. Jocher, G., et al. (2023). *YOLOv8 by Ultralytics*. https://github.com/ultralytics/ultralytics
2. Redmon, J., et al. (2016). *You Only Look Once: Unified, Real-Time Object Detection*. CVPR.
3. He, K., et al. (2016). *Deep Residual Learning for Image Recognition*. CVPR.
4. Bergmann, P., et al. (2019). *MVTec AD — A Comprehensive Real-World Dataset for Unsupervised Anomaly Detection*. CVPR. https://www.mvtec.com/company/research/datasets/mvtec-ad/
5. Tabernik, D., et al. (2019). *Segmentation-Based Deep-Learning Approach for Surface-Defect Detection*. Journal of Intelligent Manufacturing. (KolektorSDD)
6. Ultralytics. (2024). *YOLOv8 Documentation*. https://docs.ultralytics.com/

## 七、差异化小结

本方案与同类"钢材缺陷分类"方案的核心差异在于：**以通用工业零部件为研究对象、以 MVTec AD 为主数据集、以目标检测（定位+分类）为任务范式**，并构建完整 B/S 业务闭环，辅以多模型协同扩展。研究对象、数据集、任务范式三者共同构成清晰的选题边界，确保方案的独立性与技术深度。


