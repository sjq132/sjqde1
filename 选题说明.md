# 选题说明

## 一、拟定题目

# 基于 YOLOv8 的通用工业零部件多实例缺陷定位与分类系统
## 二、选题背景与意义

工业制造场景中，零部件种类繁多、外观差异大，传统人工质检效率低且主观性强。现有多数视觉质检方案聚焦于**单一材料的表面纹理缺陷**（如钢材划痕、PCB 污点），以整图分类或像素级分割为主，难以应对一张图像中包含**多个不同零件实例**的复杂产线场景。

本课题提出一种**通用工业零部件多实例缺陷定位与分类系统**：以 MVTec AD 数据集（15 类工业对象）为核心，将原有像素级异常标注重构为 YOLO 检测框格式，采用 YOLOv8 单阶段目标检测模型，实现一张图中**多个零部件实例的同步定位（bounding box）与缺陷分类（正常/异常）**。系统以 Python 本地工具形式交付，输出标注图、结构化检测报告（JSON/CSV）及统计图表，覆盖从数据预处理、模型训练到结果可视化的完整流程。

## 三、完成目标

1. 构建一个基于深度学习的**通用工业零部件**表面缺陷自动检测系统（区别于单一材料/钢材缺陷分类方案）
2. 实现对常见缺陷（划痕、裂纹、凹陷、异物、缺失部件等）的自动**定位与分类**
3. 提供 Web 可视化界面，支持图片上传、检测结果展示（检测框+类别+置信度）、历史记录查询
4. 检测模型在测试集上 mAP@0.5 达到可用水平，整体形成可演示的业务闭环


 ## 四、涉及的技术方向

本项目涉及以下 **4 个**上学期课程专题的技术方向：

| 序号 | 课程专题技术方向 | 在本项目中的具体应用 |
|------|----------------|-------------------|
| 1 | **计算机视觉 / 数字图像处理** | 对工业零部件图像进行预处理（去噪、增强、尺寸归一化），理解缺陷区域的纹理与边缘特征，为检测模型提供高质量输入 |
| 2 | **机器学习 / 深度学习** | 使用 YOLOv8 目标检测模型完成缺陷定位与分类，ResNet-18 做二次分类验证；涉及数据增强、迁移学习、模型微调等核心技术 |
| 3 | **智能优化算法** | 在模型训练阶段应用优化思想（如学习率调度策略、超参数搜索、数据增强策略优化），理解梯度下降、动量优化等智能优化方法在深度学习训练中的核心作用 |
| 4 | **Python 程序设计** | 使用 Python 编写数据预处理脚本、标注格式转换工具（MVTec AD 像素 mask → YOLO bbox）、训练流程控制程序；涉及文件目录遍历、列表/字典数据组织、函数模块化封装、第三方库调用等 |

 。

## 五、与同类选题的差异说明

| 对比维度 | 同类"表面缺陷检测"选题（典型） | 本课题 |
|---------|---------------------------|--------|
| 任务形式 | 整图分类 或 像素级分割（异常区域掩码） | **多实例目标检测**（边界框定位 + 分类） |
| 检测粒度 | 图像级 / 像素级（surface-level） | **实例级**（instance-level，一张图多个零件各自定位） |
| 对象范围 | 单一材料/产品（如钢材、PCB） | **15 类通用工业零部件**（瓶、胶囊、螺母、拉链等） |
| 核心模型 | 传统 CV / U-Net / ResNet 分类 | **YOLOv8 单阶段检测**（兼顾速度与精度） |
| 数据来源 | 单一领域数据集或自制数据 | **MVTec AD**（权威跨域数据集，标注从分割 mask 重构为检测框） |
| 输出形式 | 分割图 / 类别标签 | **标注图 + JSON/CSV 报告 + 统计图表** |

> 本课题与"基于机器视觉的工业产品表面缺陷智能检测系统"类选题的核心区别在于：后者多以**表面纹理异常**为检测目标，输出为整图类别或像素级分割掩码；本课题以**多实例零件的定位与分类**为任务，输出为每张图中每个零件实例的边界框与缺陷类别，属于 instance-level 工业质检，而非 surface-level 异常分割。

**结论**：本选题虽同属"工业视觉质检"大方向，但研究对象为通用零部件、数据集与任务范式均不同，方案具有独立的选题边界与技术深度。
