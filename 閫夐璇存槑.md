# 选题说明

## 一、拟定题目

**工业零部件质量智能检测系统**

## 二、完成目标

1. 构建一个基于深度学习的工业零部件表面缺陷**自动检测系统**，而非仅做缺陷分类——系统需同时输出缺陷的**位置（检测框）+ 类别 + 置信度**。
2. 实现对热轧带钢等工业零部件表面六类典型缺陷（裂纹 crack、夹杂物 inclusion、斑块 patch、麻点 pitted_surface、氧化皮 rolled-in_scale、划痕 scratch）的自动定位与分类。
3. 提供完整的 B/S 架构 Web 应用，实现"图像上传 → 智能检测 → 结果标注展示 → 历史存储 → 统计查询"的业务闭环。
4. 检测模型在 NEU-DET 测试集上达到 mAP@0.5 ≥ 0.80 的目标，并保证推理速度满足准实时需求（CPU 下单张推理 < 1s）。

## 三、涉及的技术方向

本项目至少涉及以下 **4 个**上学期课程专题的技术方向，并说明在本项目中的具体应用：

| 序号 | 课程专题技术方向 | 在本项目中的具体应用 |
|------|----------------|-------------------|
| 1 | **计算机视觉 / 数字图像处理** | 对工业零部件灰度图像进行预处理（去噪、对比度增强、尺寸归一化至 640×640）；理解卷积特征提取、边界框回归、非极大值抑制（NMS）等视觉算法原理 |
| 2 | **机器学习 / 深度学习** | 使用 YOLOv8 单阶段目标检测模型完成缺陷定位与分类；涉及迁移学习（预训练权重微调）、数据增强（翻转/缩放/亮度扰动）、置信度阈值过滤等核心技术 |
| 3 | **软件工程 / Web 开发** | 采用 Flask 构建后端服务，HTML+CSS+Bootstrap+JavaScript 构建前端交互界面；遵循模块化分层设计（路由层 / 服务层 / 算法层 / 数据层）与迭代开发流程 |
| 4 | **数据库技术** | 使用 SQLite 存储每次检测记录（图片路径、检测时间、缺陷类型、置信度、检测框坐标），支持按时间/类型/置信度等条件筛选查询与统计 |

> 以上四个方向分别对应《数字图像处理》《机器学习导论》《软件工程》《数据库系统》等上学期课程的核心专题内容。本项目以"目标检测 + Web 工程闭环"为主线，将四门课程的知识串联落地。

## 四、选题差异化说明（重点）

本选题与同类"钢材/金属表面缺陷检测"方案的核心差异在于——**不少同类方案仅将缺陷识别建模为"图像分类"问题（整张图判一个类别），而本系统采用"目标检测"范式，要求模型在输出类别的同时精确定位缺陷所在区域（bounding box）**，更贴近产线"定位缺陷位置 + 判定缺陷类型"的实际质检需求。

具体差异化体现在：

| 对比维度 | 同类"缺陷分类"方案 | **本系统（缺陷检测）** |
|---------|------------------|---------------------|
| 任务形式 | 整图 → 单类别标签 | 图像 → 多目标检测框 + 类别 + 置信度 |
| 输出信息 | 仅类别 | 类别 + 位置坐标 + 置信度 |
| 工程闭环 | 多为算法脚本/Notebook | 完整 B/S 系统：上传→检测→标注→存储→查询 |
| 模型协同 | 单模型 | YOLOv8 检测 +（可选）ResNet-18 二次验证 + Qwen2.5-VL 报告生成 |

该差异化定位既保证了选题的工程价值，也为后续扩展（如缺陷尺寸测量、多目标统计、自然语言检测报告）预留了接口。
